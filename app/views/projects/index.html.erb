<h1>Projects</h1>


  <% @projects.each do |project| %>
    <h3><%= link_to project.name, project %></h3>
    <p><%= project.description %></p>
    <p><%= project.staffs.map { |s| s.name_full }.join(', ') %></p>
  <% end %>

<%= link_to 'New Project', new_project_path %>
